# 安全政策

## 支持范围

当前只维护默认分支 `main` 的最新版本。安全修复会优先合入默认分支，不为旧提交单独维护补丁分支。

## 报告安全问题

请不要通过公开 Issue、Discussion、截图或日志披露漏洞、API Key、客户资料、合同、财务数据或可复现的敏感样本。

请在仓库的 **Security** 页面选择 **Report a vulnerability**，通过 GitHub 私密漏洞报告提交以下信息：

- 受影响的版本或提交；
- 问题描述和影响范围；
- 最小复现步骤；
- 建议修复方式（如有）。

维护者会尽快确认问题。在修复发布前，请避免公开漏洞细节。

## 本地数据边界

本项目是单机桌面应用，业务数据库、附件和 AI 配置默认保存在本机。提交问题或测试用例时，请使用虚构数据，并确认以下文件未进入 Git：

- `config.ini`、`.env` 和任何密钥文件；
- `*.db`、数据库备份和日志；
- `attachments/`；
- 含客户、供应商、合同、发票或人员信息的导出文件。
